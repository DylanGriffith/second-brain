SELECT * FROM users LIMIT 10;
SELECT id, email, created_at FROM users WHERE active = true ORDER BY created_at DESC;
INSERT INTO users (email, name) VALUES ('test@example.com', 'Test User');
UPDATE users SET active = false WHERE last_login < NOW() - INTERVAL '90 days';
DELETE FROM sessions WHERE expires_at < NOW();
SELECT COUNT(*) FROM orders WHERE status = 'pending';
SELECT u.email, COUNT(o.id) as order_count FROM users u LEFT JOIN orders o ON o.user_id = u.id GROUP BY u.email;
CREATE INDEX CONCURRENTLY idx_users_email ON users(email);
EXPLAIN ANALYZE SELECT * FROM orders WHERE user_id = 42;
\dt
